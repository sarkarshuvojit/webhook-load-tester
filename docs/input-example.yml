version: v1

tests:
  - name: test-api-1
    url: http://localhost:8080/api1
    body: "{\"message\": \"ok\"}"
    injectors:
      correlationInjector:
        path: "headers.webhook-reply-to"
        use: UUID
    pickers:
      correlationPicker:
        path: "body.correlationId"

run:
  iterations: 50
  durationSeconds: 10

outputs:
  - type: text
    path: out.txt
